mutation createFolder($input: CreateFolderInput!) {
  createFolder(input: $input) {
    folder {
      id
      encryptedName
      encryptedNameNonce
      subkeyId
      parentFolderId
      rootFolderId
      workspaceId
    }
  }
}
