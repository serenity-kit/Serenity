mutation createFolder($input: CreateFolderInput!) {
  createFolder(input: $input) {
    folder {
      id
      name
      encryptedName
      encryptedNameNonce
      subkeyId
      parentFolderId
      rootFolderId
      workspaceId
    }
  }
}
