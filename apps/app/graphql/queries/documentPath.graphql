query documentPath($id: ID!) {
  documentPath(id: $id) {
    id
    encryptedName
    encryptedNameNonce
    workspaceKeyId
    parentFolderId
    rootFolderId
    workspaceId
    subkeyId
    keyDerivationTrace {
      workspaceKeyId
      parentFolders {
        folderId
        subkeyId
        parentFolderId
      }
    }
  }
}
