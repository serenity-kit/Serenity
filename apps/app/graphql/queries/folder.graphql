query folder($id: ID!) {
  folder(id: $id) {
    id
    encryptedName
    encryptedNameNonce
    parentFolderId
    workspaceId
    keyDerivationTrace {
      workspaceKeyId
      subkeyId
      parentFolders {
        folderId
        subkeyId
        parentFolderId
      }
    }
  }
}
