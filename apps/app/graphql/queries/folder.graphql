query folder($id: ID!) {
  folder(id: $id) {
    id
    nameCiphertext
    nameNonce
    parentFolderId
    workspaceId
    keyDerivationTrace {
      workspaceKeyId
      trace {
        entryId
        subkeyId
        parentId
        context
      }
    }
  }
}
