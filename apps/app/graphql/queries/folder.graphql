query folder($id: ID!) {
  folder(id: $id) {
    id
    encryptedName
    encryptedNameNonce
    parentFolderId
    workspaceId
    keyDerivationTrace {
      workspaceKeyId
      trace {
        entryId
        subkeyId
        parentId
        context
      }
    }
  }
}
