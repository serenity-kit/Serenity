query unauthorizedMember {
  unauthorizedMember {
    userId
    workspaceId
    devices {
      userId
      signingPublicKey
      encryptionPublicKey
      info
      createdAt
      encryptionPublicKeySignature
    }
  }
}
