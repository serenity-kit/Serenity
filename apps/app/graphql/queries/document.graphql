query document($id: ID!) {
  document(id: $id) {
    id
    encryptedName
    encryptedNameNonce
    workspaceKeyId
    subkeyId
    contentSubkeyId
    parentFolderId
    workspaceId
    nameKeyDerivationTrace {
      workspaceKeyId
      subkeyId
      parentFolders {
        folderId
        subkeyId
        parentFolderId
      }
    }
  }
}
