query document($id: ID!) {
  document(id: $id) {
    id
    encryptedName
    encryptedNameNonce
    parentFolderId
    workspaceId
    nameKeyDerivationTrace {
      workspaceKeyId
      subkeyId
      parentFolders {
        folderId
        subkeyId
        parentFolderId
      }
    }
  }
}
