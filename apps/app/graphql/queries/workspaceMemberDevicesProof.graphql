query workspaceMemberDevicesProof($workspaceId: ID!) {
  workspaceMemberDevicesProof(workspaceId: $workspaceId) {
    proof {
      hash
      hashSignature
      clock
      version
    }
    serializedData
  }
}
