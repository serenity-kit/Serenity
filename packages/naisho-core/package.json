{
  "name": "@naisho/core",
  "version": "0.0.1",
  "main": "src/index",
  "scripts": {
    "build": "rimraf -rf ./dist && yarn build:xstate-types && tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build",
    "test": "jest",
    "ts:check": "yarn tsc --noEmit",
    "lint": "eslint --ext .tsx --ext .ts ./",
    "build:xstate-types": "xstate typegen \"./**/*.ts?(x)\""
  },
  "dependencies": {
    "canonicalize": "^1.0.8",
    "libsodium-wrappers": "^0.7.10",
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "@babel/core": "^7.19.3",
    "@babel/preset-env": "^7.19.3",
    "@babel/preset-typescript": "^7.18.6",
    "@types/jest": "^29.1.1",
    "@types/libsodium-wrappers": "^0.7.9",
    "jest": "^29.1.1",
    "mock-socket": "^9.2.1"
  },
  "jest": {
    "setupFilesAfterEnv": [
      "<rootDir>/test/config/jestTestSetup.ts"
    ]
  }
}
